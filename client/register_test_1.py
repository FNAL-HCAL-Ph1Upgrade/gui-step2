# Register Test 1
for bit in range(8):
    a = 2**bit
    print "Bit ", bit, " -> Hex ", hex(a),  " -> Dec ", a

# 1. Determine address of Fanout (0x72 unless switches are flipped).

# 2. Determine bits to set on Fanout to open i2c with ngCCM FPGA Bridge (0x74). Verify address of Fanout.

# 3. Determine
